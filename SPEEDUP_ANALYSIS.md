# 答题提速方案分析

> 分析日期：2026-07-13
> 版本：v0.4.0（纯本地离线版）
> 分析方式：基于源码的静态流程拆解 + 时序估算

---

## 一、当前答题流程耗时拆解

### 1.1 单次答题（SolvePipeline.solve）

| 阶段 | 操作 | 估算耗时 | 说明 |
|------|------|---------|------|
| L1 | Excel 题库 trigram 搜索 | **~1-3 ms** | 纯 CPU，500 条数据约 1-3ms |
| L2 | Wiki FTS4 + trigram 搜索 | **~10-25 ms** | SQLite FTS 索引 + 全表 trigram 扫描 |
| 未命中 | 返回错误提示 | **~1 ms** | 无额外计算 |

**单次答题总耗时：~2-30 ms（纯本地，极快）**

### 1.2 多轮自动答题（MultiRoundRunner）— 每页耗时

多轮答题是主要使用场景，每页（每道题）的完整流程：

```
┌─────────────────────────────────────────────────────────────┐
│  1. 提取当前页面文本  (~1900 ms)                              │
│     ├─ delay(400) 等待页面渲染                                │
│     ├─ backward scroll + delay(300) ×1                       │
│     └─ forward scroll + delay(300) ×2 + 遍历节点提取文字       │
├─────────────────────────────────────────────────────────────┤
│  2. L1 题库匹配       (~2 ms)                                │
├─────────────────────────────────────────────────────────────┤
│  3. 点击填入答案      (~3500 ms, 1题)                         │
│     └─ delay(1500 × 题数 + 2000)                             │
├─────────────────────────────────────────────────────────────┤
│  4. 翻页到下一题      (~1400 ms)                              │
│     ├─ click "下一页" + delay(600)                           │
│     ├─ delay(PAGE_WAIT=800)                                  │
│     └─ 验证翻页成功（重试最多3次）                            │
├─────────────────────────────────────────────────────────────┤
│  每页总计：~6800 ms（6.8 秒/题）                              │
│  100 题考试预估：~11 分钟                                    │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 瓶颈分布

| 环节 | 耗时占比 | 当前值 | 优化空间 |
|------|---------|--------|---------|
| **文本提取（滚动+等待）** | ~28% | ~1900 ms | 大 |
| **答案填入等待** | ~51% | ~3500 ms | 大 |
| **翻页等待** | ~21% | ~1400 ms | 中 |
| 题库匹配 | ~0% | ~2 ms | 小 |

**结论：提速的关键不在"匹配算法"，而在"UI 交互等待时间"和"文本提取效率"。**

---

## 二、提速方案矩阵

### 2.1 方案一：缩减 UI 等待时间（高影响 × 低难度）

#### O1-1 缩短答案填入后的等待时间

**当前代码**（`MultiRoundRunner.kt:256`）：
```kotlin
delay(1500L * answerCount + 2000L)  // 1题=3500ms, 2题=5000ms
```

**问题**：这是为了"等待自动点击完成"，但实际上 AccessibilityService 中的 `performAutoClick` 已经包含了自己的内部等待（`delay(1500)` 每次点击后）。这造成了**双重等待**。

**优化方向**：
- 将 MultiRoundRunner 的等待改为基于事件通知（AccessibilityService 点击完成后发事件），而非固定 delay
- 或至少缩短为 `delay(800L * answerCount + 500L)`，依靠 AccessibilityService 内部的 delay 兜底

**预期收益**：1 题从 3500ms → ~1300ms，**节省 ~2200ms/题**

#### O1-2 缩短翻页等待时间

**当前代码**：
```kotlin
delay(600)      // clickNextPage 内部
delay(800)      // PAGE_WAIT_MS
```

**优化方向**：
- `PAGE_WAIT_MS` 从 800ms 降至 400-500ms
- 翻页验证重试的 delay 从 500ms 降至 200ms
- 利用"翻页动画一般 200-300ms"的特性，不用等页面完全静止

**预期收益**：每页翻页从 ~1400ms → ~900ms，**节省 ~500ms/页**

#### O1-3 缩短文本提取等待时间

**当前代码**（`ExamAccessibilityService.kt:143-223`）：
```kotlin
delay(400)              // 等待渲染
delay(300)              // backward scroll 后
delay(300) × 3         // 3轮 forward scroll
```

**优化方向**：
- `delay(400)` → `delay(200)`（现代手机渲染足够快）
- 3 轮 scroll 压缩为 2 轮（大多数考试页面不需要 3 轮就能覆盖全部内容）
- scroll 后的 delay 从 300ms → 200ms

**预期收益**：从 ~1900ms → ~1000ms，**节省 ~900ms/页**

**O1 合计收益**：每页从 ~6800ms → ~3200ms，**提速约 2.1 倍**

---

### 2.2 方案二：流水线并行化（高影响 × 中难度）

#### O2-1 "翻页+提取"重叠执行

**当前模式**：串行 — 翻页 → 等页面稳定 → 提取文本 → 匹配 → 填入 → 翻页

**优化模式**：
```
时间轴 ──────────────────────────────────────►

第 N 页：   [提取] [匹配] [填入]
                              ↓
第 N+1 页：                   [翻页] [提取] [匹配] [填入]
```

**更激进的模式**：在填入第 N 页答案的同时，就开始翻页并提取第 N+1 页：
```
第 N 页：   [提取] [匹配] [填入开始]          [填入完成]
                              ↓              ↓
第 N+1 页：                   [翻页] [提取]  [匹配] [填入开始]
```

**实现要点**：
- 填入答案后立即触发翻页（不等 `delay(FILL_WAIT_MS)` 结束）
- 翻页后不等完全稳定就开始提取（减少 `delay(PAGE_WAIT_MS)`）
- 提取和匹配可以在后台协程中并发执行

**预期收益**：每页实际占用时间从 ~6800ms → ~4500ms，**提速约 1.5 倍**

#### O2-2 预提取下一页

**思路**：在解答当前页的同时，通过无障碍服务的窗口监听（`onAccessibilityEvent`）或定时轮询，**预判**下一页的内容是否已加载。

**实现要点**：
- 利用 `AccessibilityEvent.TYPE_WINDOW_CONTENT_CHANGED` 监听页面变化
- 页面变化事件触发后立即开始提取，不用等固定 delay
- 需要处理"事件抖动"（连续多次变化事件）

**预期收益**：减少翻页后等待时间 ~300-500ms

---

### 2.3 方案三：匹配算法优化（中影响 × 低难度）

#### O3-1 题库搜索 TopN 调优

**当前代码**（`KnowledgeBase.search()`）：
```kotlin
fun search(query: String, options: String = "", topN: Int = 300): List<Pair<KBEntry, Float>>
```

SolvePipeline 调用时 `topN = 5`，但 `search()` 内部默认 `topN = 300`，会排序 300 个结果。

**优化**：
- 将 `search()` 的默认 `topN` 从 300 改为 20-50
- 或让调用方传更小的值（`search(text, topN = 20)`）
- 提前终止阈值调得更激进（`EARLY_TERM_FACTOR` 从 0.05 提高到 0.08）

**预期收益**：匹配耗时从 ~2ms → ~1ms（微优化，但零风险）

#### O3-2 减少 LCS 计算

**当前代码**：对 score 在 0.15-0.50 之间的条目执行 LCS（最长公共子序列）：
```kotlin
if (score in 0.15f..0.50f) {
    val lcsRatio = KBEntry.computeLCSRatio(...)
    ...
}
```

LCS 是 O(n×m) 算法，对长文本开销较大。

**优化**：
- LCS 只在高置信度候选上执行（如 trigram score ≥ 0.30 时）
- 或完全移除 LCS，依赖 trigram + bigram + token 的混合分数已经足够

**预期收益**：对命中 LCS 分支的查询节省 ~1-5ms

---

### 2.4 方案四：无障碍操作优化（中影响 × 中难度）

#### O4-1 减少滚动轮次

**当前代码**：`extractCurrentPageOnly()` 执行 1 次 backward + 2 次 forward scroll。

**优化**：
- 先尝试不滚动直接提取（很多考试页面单屏显示完整题目）
- 如果提取到的内容包含完整的题号+选项模式，就跳过滚动
- 只有内容不完整时才触发滚动

**预期收益**：单屏页面从 ~1900ms → ~400ms，**节省 ~1500ms**

#### O4-2 批量点击选项

**当前模式**：AccessibilityService 逐个点按答案（每题多次点击，每次间隔 1500ms）。

**优化**：
- 找到页面上所有答案对应的节点，一次性批量 performAction
- 或并行点击（多个 AccessibilityNodeInfo 同时触发 ACTION_CLICK）
- 减少 `delay(1500)` 的串行等待

**预期收益**：多题页面可节省大量时间

---

### 2.5 方案五：预热与缓存（低影响 × 低难度）

#### O5-1 题库匹配结果缓存

**思路**：同一道题目在多轮答题中可能被多次匹配（如用户手动重试、多轮模式反复扫描）。

**实现**：
```kotlin
// 在 SolvePipeline 或 KnowledgeBaseManager 中
private val answerCache = LruCache<String, String>(maxSize = 100)
```

**预期收益**：重复题目匹配从 ~2ms → 缓存命中 ~0.1ms（微优化）

#### O5-2 App 启动时预热 featureCache

**当前**：`featureCache` 在 `init()` 中已构建，但加载 JSON 后可能未完全预热。

**优化**：确保 `load()` 后调用 `buildFeatureCache()`，并加入启动进度提示。

**预期收益**：首次搜索不触发 on-the-fly 构建，避免 ~5-50ms 的额外开销

---

## 三、优先实施建议

### 第一阶段（立即实施，最快 ROI）

| # | 优化 | 修改文件 | 预期提速 | 风险 |
|---|------|---------|---------|------|
| 1 | `PAGE_WAIT_MS` 800→400 | `MultiRoundRunner.kt` | ~400ms/页 | 低 |
| 2 | `FILL_WAIT_MS` 公式 1500×N+2000 → 800×N+500 | `MultiRoundRunner.kt` | ~2200ms/题 | 中（需测试稳定性） |
| 3 | 提取 delay 400→200, scroll delay 300→200 | `ExamAccessibilityService.kt` | ~700ms/页 | 低 |
| 4 | 减少 forward scroll 从 2 轮→1 轮 | `ExamAccessibilityService.kt` | ~500ms/页 | 中（可能漏内容） |

**第一阶段合计**：每页从 ~6800ms → ~3000ms，**提速约 2.3 倍**

### 第二阶段（验证后实施）

| # | 优化 | 修改文件 | 预期提速 | 风险 |
|---|------|---------|---------|------|
| 5 | 填入与翻页重叠执行 | `MultiRoundRunner.kt` | ~1000ms/页 | 中 |
| 6 | 不滚动直接提取（智能判断） | `ExamAccessibilityService.kt` | ~1000ms/页 | 中 |
| 7 | 批量点击选项 | `ExamAccessibilityService.kt` | ~500ms/页 | 高（需兼容各种考试UI） |
| 8 | search() topN 300→30 | `KnowledgeBaseManager.kt` | ~1ms/次 | 低 |

---

## 四、当前匹配算法性能（供参考）

| 数据规模 | L1 搜索耗时 | L2 搜索耗时 | 说明 |
|---------|-----------|-----------|------|
| 500 条 | ~1-3 ms | ~10-25 ms | 当前典型规模 |
| 5000 条 | ~5-10 ms | ~50-120 ms | 线性增长 |
| 20000 条 | ~20-40 ms | ~200-500 ms | 需关注 |

**匹配算法本身不是瓶颈**。即使 5000 条数据，L1 也仅 5-10ms，远低于 UI 等待时间（秒级）。

---

## 五、总结

纯本地版的最大提速空间在 **UI 交互等待时间**，而非匹配算法：

1. **当前每页 ~6.8 秒**，100 题约 11 分钟
2. **通过缩减 delay 可降至 ~3 秒/页**，100 题约 5 分钟
3. **通过流水线可进一步降至 ~2 秒/页**，100 题约 3 分钟
4. 匹配算法即使优化到 0ms，对总时间影响也 <1%

**推荐立即实施的修改**：
- `MultiRoundRunner.kt`: `PAGE_WAIT_MS = 400`, `FILL_WAIT` 公式缩短
- `ExamAccessibilityService.kt`: 提取 delay 缩短，滚动轮次减少
